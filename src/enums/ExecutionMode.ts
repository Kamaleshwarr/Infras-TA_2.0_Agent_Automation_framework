/**
 * Framework execution modes mapped to npm scripts and environment settings.
 */
export enum ExecutionMode {
  HEADLESS = 'headless',
  HEADED = 'headed',
  PARALLEL = 'parallel',
  CROSS_BROWSER = 'cross-browser',
  RETRY = 'retry',
}
